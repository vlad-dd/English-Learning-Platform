import styled from "styled-components";
import { Space, Row } from "antd";

export const StyledListElement = styled.p``;

export const StyledSpace = styled(Space)`
  display: flex;
  flex-direction: row;
`;

export const StyledRow = styled(Row)`
  padding-top: 10px;
`;