import styled from 'styled-components';
import { Layout } from 'antd';

const { Sider } = Layout;

export const LogoContainer = styled.div`
  overflow: hidden;
  cursor: pointer;
`;

export const StyledLayoutSider = styled(Sider)`
  background: #010a12;
`;